using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Kernel.core;

public class EffectProcessorComponent : IComponent
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
